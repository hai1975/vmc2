# Google Gemini API key (server-side only — used to mint ephemeral Live tokens)
GEMINI_API_KEY=

# Optional: Live model riêng cho MC tiếng Việt (Chương trình 2). Để trống = dùng GEMINI_LIVE_MODEL.
# GEMINI_LIVE_MODEL_VI=gemini-2.5-flash-native-audio-preview-12-2025
# Recommended: gemini-3.1-flash-live-preview
GEMINI_LIVE_MODEL=gemini-3.1-flash-live-preview
GEMINI_LIVE_MODEL_NL=gemini-2.5-flash-native-audio-preview-12-2025
# Document OCR (Scan button) — gemini-2.0-flash was shut down Jun 2026
GEMINI_VISION_MODEL=gemini-2.5-flash

# n8n webhook for toolbar Email button (multipart: to, subject, message, attachment)
N8N_EMAIL_WEBHOOK_URL=https://hai1975.app.n8n.cloud/webhook/send-email

# Azure Speech / Voice Live (Server 2)
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=westeurope
AZURE_VOICELIVE_MODEL=gpt-realtime-mini
AZURE_VOICELIVE_ENDPOINT=

# Paths (relative to project root)
FORM_DIR=Form
SCHEMA_DIR=schemas
DATA_DIR=data

# Database
DATABASE_URL=sqlite:///./data/vmc.db

# SMTP (optional — for sending PDF on submit; can also configure in app Settings UI)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

CORS_ORIGINS=http://localhost:5173,http://localhost:5174,capacitor://localhost,http://localhost,https://hai1975.com,https://www.hai1975.com
